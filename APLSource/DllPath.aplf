 dll←DllPath name;dll;extra;path;prefix

 prefix←('npoi' 'gembox'⍳⊂⎕C HELPERLIB)⊃'NPOI' 'GemBox'
 path←PATH,'/',prefix,'Helpers/bin/Debug/'

 :If 0∊⍴name
     dll←path,'NPOI.dll'
 :Else
     dll←path,name,'.dll'
 :EndIf
